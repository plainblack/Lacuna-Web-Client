'use strict';

var React = require('react');

var RightSidebar = React.createClass({
    render: function() {
        return (
            <div></div>
        );
    }
});

module.exports = RightSidebar;
